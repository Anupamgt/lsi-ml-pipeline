# Preview maps (JPEG)

These images are for **viewing in the browser**. They are not georeferenced analysis files.

- Phase 1 results start with `P1_01_LSI_hazard_zones.jpg`.
- Sonker factor screenshots start with `S17_`.
- For GIS work, use the matching GeoTIFF in [`../geotiff/`](../geotiff/).

Back to the pack: [`../../README.md`](../../README.md)
